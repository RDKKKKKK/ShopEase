apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  labels:
    app: user-service
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 29000
      targetPort: 29000
  type: ClusterIP